import React from 'react';
import { OnboardingStepBox, OnboardingStepBoxProps } from '@firmware-components';

const PinStepBox = (props: OnboardingStepBoxProps) => {
    return <OnboardingStepBox image="PIN" {...props} />;
};

export default PinStepBox;
